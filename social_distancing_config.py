# base path to YOLO directory
MODEL_PATH = "yolo-coco"

# initialize minimum probability and non-maxima suppression
MIN_CONF = 0.3
NMS_THRESH = 0.3

#  if NVIDIA CUDA GPU is used
USE_GPU = False

# define the minimum safe distance (in pixels) that two people can be
# from each other
MIN_DISTANCE = 100
